import { TouchableOpacity, View } from "react-native";

export default function OutGoingOrderTab() {
  return (
    <TouchableOpacity
      style={{
        width: "93%",
        height: 56,
        backgroundColor: "whitesmoke",
        borderRadius: 12,
      }}
    >
      <View></View>
    </TouchableOpacity>
  );
}
